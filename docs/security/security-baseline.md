# Security Baseline

Before production release, NEXUS must enforce authentication, RBAC, tenant isolation, rate limits, input validation, secure cookies/tokens, audit logging, secret management, and encrypted transport.
